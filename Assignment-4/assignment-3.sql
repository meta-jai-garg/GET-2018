-- adding index on product table

CREATE INDEX `product_index` ON `product`(`title`);

-- adding index on product table

CREATE INDEX `product_index` ON `product`(`title`);

-- adding index on product table

CREATE INDEX `product_index` ON `product`(`title`);